/** Main column for creator dashboard pages — wide enough for the full tab bar on one line. */
export const DASHBOARD_MAIN_MAX_W_CLASS = "max-w-[996px]";

export const DASHBOARD_MAIN_SHELL_CLASS = `mx-auto flex min-h-screen ${DASHBOARD_MAIN_MAX_W_CLASS} flex-col px-4 py-12`;

export const DASHBOARD_MAIN_SHELL_FALLBACK_CLASS = `mx-auto flex min-h-[min(420px,50vh)] ${DASHBOARD_MAIN_MAX_W_CLASS} flex-col justify-center px-4 py-12`;

export const DASHBOARD_LOADING_SHELL_CLASS = `mx-auto flex min-h-[50vh] ${DASHBOARD_MAIN_MAX_W_CLASS} flex-col items-center justify-center px-4 py-16`;
