/** Dark storefront-aligned Stripe Checkout (embedded + hosted). */
export const storefrontStripeCheckoutBranding = {
  background_color: "#09090b",
  button_color: "#1e40af",
  /** Slightly denser fields than rounded. */
  border_style: "rectangular" as const,
  font_family: "inter" as const,
};
